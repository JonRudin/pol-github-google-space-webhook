apiVersion: v1
kind: Service
metadata:
  name: pol-github-google-space-webhook
  namespace: pol-github-google-space-webhook
spec:
  selector:
    app: pol-github-google-space-webhook
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
